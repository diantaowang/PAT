PAT 1054. The Dominant Color (20)

一共提交五个源文件:

(1). PAT_1054.c: Moore voting algorithm -- 每找出两个不同的element，就成对删除即count--，

     最终剩下的一定就是所求的. 时间复杂度: O(n). AC. 

(2). PAT_1054.cpp: 采用sort()函数排序，输出中间数. 不稳定，有时会超时^-^，AC.

(3). PAT_1054_new.c: 随机概率选取，大概率时间，AC.

(4). PAT_1054_old.c: 查找中位数算法，时间复杂度: O(n). (PS: 仅适用于互异数组，《算法导论》第九章.)

(5). PAT_1050_t.c: 查找中位数算法，测试点2超时. (PS: 存在大量重复元素时效率过低.)
